---
"@glyphkiln/core": minor
---

Add the public `@glyphkiln/core/scene` Scene Kernel v1 contract for strict,
deterministic expert scenes with SVG/PNG outputs, scene manifests, fingerprints,
groups, closed transforms and clips, explicit-route connectors with validated
semantic endpoints, reading order, and Core-owned outline or selectable-text
layout. Scene preflight enforces a closed path grammar, XML-safe text, bounded
canvas area, serialization-safe geometry precision, and strictly validated
reproduction evidence. Advance the shared renderer identity to `0.5.0`;
existing semantic SVG and PNG bytes stay exact while their fingerprints and
manifests record the new identity.
