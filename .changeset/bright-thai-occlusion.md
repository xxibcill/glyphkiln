---
"@glyphkiln/core": patch
---

Warn when a later opaque Scene image or solid rectangle covers fitted text, with both element IDs and overlap area. Check PNG alpha in the visible crop and allow deliberate overlays to be marked through render options. SVG and PNG pixels remain unchanged.
