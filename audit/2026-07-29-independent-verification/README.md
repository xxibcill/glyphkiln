# Audit evidence chronology

The files in this directory are immutable historical evidence from the
2026-07-29 independent verification cycle. Statements that the package remained
`0.1.0` describe the audited pre-release commit. The remediation closed all
tracked findings, after which the signed `v0.2.0` source release applied the
pending minor Changeset.

The work targeting package `0.3.0` was implemented after that audit. It adds
deterministic text-layout capability diagnostics while preserving the accepted
`0.2.0` renderer pixels and fingerprints. This note clarifies chronology
without rewriting signed or dated audit reports.
